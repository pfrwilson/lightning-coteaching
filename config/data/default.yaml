
dataset: 
  
  clean_dataset:
    name: mnist
    download: true
    root: /home/paulw/data/mnist
  
  noisy_dataset:
    seed: 0
    noise_rate: 0.5
    noise_type: symmetric
    num_classes: 10

preprocessing: 
  use_augmentations: no
  scale: [0.8, 1]
  target_shape: 28

batch_size: 128